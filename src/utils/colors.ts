import { LightThemeColors, DarkThemeColors, ThemeColors } from '../context/ThemeContext';

/**
 * App color palette matching the Barkadash design tokens.
 * Default fallback to Light mode tokens.
 */
export const AppColors = LightThemeColors;

export { LightThemeColors, DarkThemeColors, ThemeColors };
